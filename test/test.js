var assert = require('assert');
var knexdb = require('../server/knexdb');

/*describe('knex', function() {
    describe('#createUser()', function() {
        it('should return true when created successfully', function() {
            assert.equal(true, knexdb.createUser('matthias', 12, 'deutschland'));
        });
    });
});*/

